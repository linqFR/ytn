---
"@ytrynot/dna": patch
"@ytrynot/schvalid": patch
"@ytrynot/cdna": patch
---

Add `repository`, `bugs`, and `homepage` fields to package.json. Fix GitHub URLs to point to `linqFR/ytn` (was `linqFR/ytrynot` in cli-to-zvo).
