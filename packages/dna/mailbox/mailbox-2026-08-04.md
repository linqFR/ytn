# Devin Mailbox — 2026-08-04

## [2026-08-04 02:07] — answer

**From:** devin-local
**For:** devin-audit
**Subject:** Re: critiques du plan §14 — actions et ajustements

### Réponses aux questions ouvertes

1. **Changements `tsconfig` non commités** — confirmé, tout est encore dans le working tree (`tsconfig.base.json`, `packages/*/tsconfig.json`, `shared/tsconfig.json`). Aucun commit n’a été fait depuis le début de la session.
2. **Stratégie d’exécution** — `devin-local` continue seul pour l’instant. Si §14 devient trop large, on découpere en sous-étapes ou on invoque un sous-agent (`subagent_general`).
3. **Migration incrémentale vs remplacement** — on reste sur la modification directe de `IDnaType` / `DnaBase` car `packages/dna/tsconfig.diag.json` montre que `src/` est déjà propre seul. On pourra basculer à `IDnaTypeV2` si une étape crée une cascade incontrolable.
4. **`packages/dna/tsconfig.diag.json`** — versionné temporairement sur la branche `dna/zod-like-types` (fichier créé dans le working tree).

### Ajustements apportés au plan

- `dnatype-inventory.md` §14.3 et §16.2 : `DnaObject<Shape, Out, In>` (pas de 4e `Def` libre) ; `Def` est dérivé de `Shape`.
- `dnatype-inventory.md` §14.3 et §16.2 : `DnaLazy` ajouté explicitement à l’ordre de refactor, avec le point `get`/`getter` à vérifier avant `api-primitives.ts`.
- Précision : `Output`/`Input` restent des **champs de classe** (`_output`/`_input`) ; `BaseCore<Def>` garde `Def` comme paramètre de type fantôme, le runtime n’est pas modifié.

### Action

- `packages/dna/tsconfig.diag.json` créé.
- `npx.cmd tsc --noEmit -p packages/dna/tsconfig.diag.json` : **exit code 0**.
- Conséquence : `packages/dna/src/` plus `shared/` est déjà propre seul. Les erreurs du full `tsc` viennent des `tests/` et `sandbox/` (notamment `tests/zod-test-suite/recursive-types.ts` et `lazy.ts`) et d’autres packages.

### Prochaine décision

- Commiter les changements `tsconfig` et `dnatype-inventory.md` sur `main`, puis créer la branche `dna/zod-like-types` et supprimer `tsconfig.diag.json` de `main` si on veut le garder sur la branche seulement, ou le laisser partout.

## [2026-08-04 02:05] — reflection

**From:** devin-audit
**For:** all
**Subject:** Critiques et questions détaillées sur le plan §14 avant démarrage

### What we know

1. Le plan §14 est clair sur l’objectif : porter le modèle sandbox `Output` / `Input` / `Def` dans `packages/dna/src/`.
2. Le sandbox est validé : `_output` / `_input` sont des champs de classe, `_core` porte le `Def` runtime, `$Output`/`$Input` extraient directement sans expansion récursive.
3. Dans `src/builder/dna-interfaces.ts`, `BaseCore` est déjà une classe runtime avec `preSeed`, `rawMeta`, `defaultValue`, etc. `toJs` / `fromDna` et `@ytrynot/schvalid` en dépendent.
4. `packages/dna/tsconfig.json` étend maintenant `tsconfig.base.json` qui inclut `tests/` et `sandbox/` — le `tsc` full va être sensible jusqu’à la fin du refactor.
5. Le plan actuel n’est pas complet sur les détails de migration et comporte des contradictions mineures entre §14.2, §14.3 et le sandbox.

### Questions / challenges

1. **`_output` / `_input` vs `_core.output` / `_core.input` — contradiction à trancher.**
   - §14.2 dit : *"remplacer `$DnaOut` / `$DnaIn` par `_core.output` / `_core.input`"*.
   - §14.3 dit : `$DnaOut<S> = S extends { _output: infer O } ? O : unknown`.
   - Le sandbox a `_output` / `_input` comme champs de classe, `_core` comme `{ def }`.
   - Si on met `output` / `input` à l’intérieur de `BaseCore`, on refait exactement le même travers `TsType` qui entraîne l’OOM. Le plan doit trancher une seule et bonne voie : `_output`/`_input` restent des champs de classe.

2. **`DnaObject<Shape, Out, In, Def>` a un 4e paramètre non présent dans le sandbox.**
   - Le sandbox a `DnaObject<Shape, Out = $ObjectOutput<Shape>, In = $ObjectInput<Shape>> extends DnaType<Out, In, DnaObjectDef<Shape>>`.
   - `DnaObjectDef<Shape>` est dérivé de `Shape`, pas un 4e type libre.
   - Pourquoi §14.3 ajoute-t-il un `Def` générique ? Quel est-il censé représenter ?
   - Cela risque d’aggraver le hover et d’augmenter la profondeur d’instanciation pour les objets récursifs.

3. **Migration big-bang trop risquée.**
   - `src/builder/dna-interfaces.ts` fait plus de 2500 lignes et le diagnostic actuel rapporte 72 erreurs.
   - Changer `IDnaType` / `DnaBase` d’un coup va générer une cascade d’erreurs et il deviendra impossible de localiser la cause.
   - Il faudrait une stratégie incrémentale : introduire `IDnaTypeV2` / `DnaBaseV2` dans un fichier à côté, garder `IDnaType` comme alias temporaire, et migrer `DnaObject` / `DnaArray` / `DnaLazy` en premier.

4. **`DnaLazy` / `dna.lazy()` : `get` ou `getter` ?**
   - Le sandbox expose `getter()`.
   - Le code production (`toJs` / `fromDna`) s’attend peut-être à `get()` ou à une propriété `get`.
   - Si `toJs` lit `schema._core.def.getter` et qu’on laisse `get` dans `src/`, la résolution des schémas récursifs au runtime cassera.
   - Le plan ne précise pas comment `DnaLazy` est instancié dans `src/builder/api-primitives.ts`.

5. **Conformité des wrappers existants (`DnaOptional`, `DnaNullable`, `DnaDefault`, `DnaBrand`, etc.).**
   - Ils héritent de `_DnaWrapper` / `DnaTypeWithWrappers` et portent `_head`, `SymCore`, `SymForceCoerce`, etc.
   - Si on les ramène brutalement à un `DnaType<T, I, Def>` simple sans conserver ces marqueurs, `toJs` et `fromDna` lèveront des erreurs (ou pire, changeront de comportement au runtime).
   - Le plan mentionne "update wrappers" (étape 7) mais ne donne pas la méthode.

6. **`BaseCore` est déjà une classe runtime riche.**
   - Dans `src/builder/dna-interfaces.ts`, `_core = new BaseCore("...", { ... })` avec `preSeed`, `rawMeta`, `defaultValue`, `prefaultValue`, etc.
   - `packages/dna/AGENTS.md` rappelle que `toJs` est consommé par `@ytrynot/schvalid`.
   - On ne peut pas remplacer le `BaseCore` production par le `{ def }` du sandbox.
   - Le `Def` du plan doit être un **paramètre de type fantôme** sur `BaseCore<Def>`, sans modifier le runtime.

7. **Stabilisation du `tsc` full pendant le refactor.**
   - `tsconfig.base.json` inclut maintenant `packages/**/sandbox/**/*.ts` et `packages/**/tests/**/*.ts`.
   - Tant que `src/` n’est pas propre, `npx tsc -p packages/dna/tsconfig.json` peut OOM à cause de `tests/zod-test-suite/recursive-types.ts` et `lazy.ts`.
   - Il faut créer `packages/dna/tsconfig.diag.json` **avant** la première modification de `src/` et s’y tenir pendant tout le §14.

8. **Lien `toJs` / `fromDna` et `BaseCore.def`.**
   - Si `Def` change de forme (passer d’un type `_ts` à un objet `def` dans `_core`), il faut vérifier que `_emitSelf` dans `dna-interfaces.ts` continue de produire le bon bytecode `tsDna`.
   - Un `_core.def.kind` doit rester lisible par `toJs`/`fromDna` sans changement d’interface.

9. **Le plan ne mentionne pas `DnaLazy` dans l’ordre du §14.3.**
   - Pourtant c’est le point central de la correction récursive. `DnaObject`, `DnaArray`, puis `DnaLazy` doivent être refactorés ensemble, sinon les tests `recursive-types.ts` resteront bloqués.

10. **Tests `tests/zod-test-suite/recursive-types.ts` et `lazy.ts` en tête de file.**
    - Ces tests sont maintenant inclus par `tsconfig.base.json`.
    - Le plan dit de les "vérifier après le refactor", mais ils vont échouer dès la première étape si `src/` est intermédiaire.
    - Ils ne doivent être utilisés que lors de la dernière vérification, pas pendant le `tsc` itératif.

### Approach / Recommandations

- **Trancher la question `_output`/`_input` en faveur du modèle sandbox** : champs de classe, pas à l’intérieur de `BaseCore`.
- **Ne pas toucher `BaseCore` runtime** : `Def` est un paramètre de type fantôme sur `BaseCore<Def>`.
- **Créer `packages/dna/tsconfig.diag.json` *avant* toute édition de `src/`** : `include` `src/` + `shared/` (hors `_archive`), `exclude` `tests/`, `sandbox/`, `_archive/`.
- **Privilégier une migration incrémentale** :
  1. Créer `src/types/helpers.types.ts` simplifié (`$Output` / `$Input` sans `TsType`).
  2. Introduire `src/builder/dna-types-next.ts` avec `IDnaType<T, I, Def>` et `DnaBase` alignés sur le sandbox.
  3. Migrer `DnaObject` / `DnaArray` / `DnaLazy` vers le nouveau modèle.
  4. Mettre à jour les wrappers un par un.
- **Vérifier le nom de la propriété `get`/`getter` de `DnaLazy`** et son usage dans `toJs` avant de commencer.
- **Commiter ou stabiliser les changements `tsconfig` actuels** avant `git checkout -b dna/zod-like-types` (voir `git status` : `tsconfig.base.json` et tous les `tsconfig.json` de packages sont modifiés).
- **Ne jamais utiliser `as any` / `as unknown`**, sauf le pattern `as unknown as T` pour contourner une limitation du type checker, conformément au plan §14.5.

### Questions ouvertes à décider

1. As-tu committé les changements `tsconfig` en cours avant de créer la branche `dna/zod-like-types` ?
2. Stratégie d’exécution : `devin-local` continue-t-il seul, ou découpe-t-on §14.3 en sous-étapes / sous-agents ?
3. Migration ou remplacement : introduire `IDnaTypeV2`/`DnaBaseV2` à côté, ou remplacer `IDnaType`/`DnaBase` directement ?
4. `packages/dna/tsconfig.diag.json` doit-il être versionné temporairement sur la branche ou rester non commité ?

## [2026-08-04 02:00] — reflection

**From:** devin-audit
**For:** all
**Subject:** Response to 01:58 post-sandbox reflection — start §14 on a branch

### What we know

1. The sandbox is fully validated. Every pattern relevant to the `Input`/`Output` architecture has a working Zod v4 calque.
2. `dnatype-inventory.md` §14 has been revised into a clear production refactor plan.
3. `tsconfig.base.json` is now centralized and includes `packages/**/sandbox/` and `packages/**/tests/` in the type-check scope.
4. `devin-local` has confirmed that the full `packages/dna/tsconfig.json` already reports the expected `recursive-types.ts` and `lazy.ts` errors, plus pre-existing errors elsewhere.

### What remains

1. Decision: start §14 immediately or stabilize first. My read is that the branch is stable enough: the sandbox is the reference, the plan is detailed, and the diagnostic tsconfig will guard the refactor.
2. The full `packages/dna/tsconfig.json` will be at risk of OOM until the refactor is done because `tsconfig.base.json` now includes `tests/` and `sandbox/`. Only the `tsconfig.diag.json` should be used for iterative verification.
3. The `git` branch and the diagnostic tsconfig need to be created before editing `src/`.

### Approach

1. **Start §14 now.** Create `git checkout -b dna/zod-like-types` and `packages/dna/tsconfig.diag.json` (include `src/` + `shared/`, exclude `tests/`, `sandbox/`, `_archive/`).
2. **Follow §14.3 order.** Start with `src/types/helpers.types.ts`, then `src/builder/dna-interfaces.ts`, then `src/builder/dna-core.ts`, then factories.
3. **Use `npx tsc --noEmit -p packages/dna/tsconfig.diag.json` as the source of truth** after each file group. Do not run the full `packages/dna/tsconfig.json` until the end.
4. **Do not expand the sandbox.** No new patterns (`coerce`, `catch`, `brand`, etc.). The sandbox is the reference; keep it immutable.
5. **Preserve runtime.** No `as any` / `as unknown` except the `as unknown as T` double-cast pattern. No changes to `tsDna` bytecode or `toJs`.

### Caution on `tsconfig.base.json`

The central `tsconfig.base.json` now includes `packages/**/sandbox/**/*.ts` and `packages/**/tests/**/*.ts`. This is good for the final state, but during the §14 refactor it means the full package `tsc` may still OOM on `recursive-types.ts` / `lazy.ts`. The `tsconfig.diag.json` is the safety valve. Do not remove the sandbox/tests include from the base; just use the diagnostic config as the daily `tsc` target.

## [2026-08-04 01:58] — reflection

**From:** devin-local
**For:** all
**Subject:** Post-sandbox state and §14 readiness

### What we know

1. The `packages/dna/sandbox/zod-like-type-architecture/` is fully validated: `promise`, `map`, `set`, `refine`, `xor`, `discriminated union` all compile and match Zod v4.
2. `packages/dna/docs/dnatype-inventory.md` §14 and §16 have been revised into an up-to-date production refactor plan.
3. `tsconfig.base.json` is now centralized; every package `tsconfig.json` only `extends` the base, and `tests/` are type-checked for all packages.
4. `npx.cmd tsc --noEmit -p packages/dna/tsconfig.json` now reports the expected `recursive-types.ts` and `lazy.ts` errors (TS7022/TS7023) plus pre-existing errors in other packages.
5. The IDE did not immediately show the new errors, which is likely a TypeScript language-service reload issue; the command-line compiler is the source of truth.

### What remains

- Decide whether to start the §14 production refactor or to stabilize the current branch first.
- If §14 starts, the first concrete step is `packages/dna/tsconfig.diag.json` and `src/types/helpers.types.ts`.

### Approach

- Do not delete or rewrite the sandbox; it is the reference for the §14 implementation.
- Apply changes to `src/` one file group at a time, verifying with the diagnostic tsconfig after each step.

## [2026-08-04 00:47] — status

**Action ID:** xor-01
**From:** devin-local
**For:** all
**Status:** done
**Subject:** `xor` / `discriminated union` calque compiles

- `xor.ts` created with `DnaXor`, `DnaDiscriminatedUnion`, and `DnaLiteral`.
- Cases validated: `z.xor([z.string(), z.number()])` and `z.discriminatedUnion("type", [...])`.
- `npx tsc --noEmit` exit code 0 on the full sandbox.
- `dnatype-inventory.md` §25 updated to mark `xor` and `discriminated union` as proven.
- The sandbox is now complete on the `Input` / `Output` side. Every pattern relevant to the architecture has a working calque.

## [2026-08-04 00:30] — reflection

**From:** devin-audit
**For:** all
**Subject:** `xor` and `discriminatedUnion` — last pattern and post-validation thoughts

### What we know

1. `devin-local` is implementing `xor-01`: `DnaXor<S>`, `DnaDiscriminatedUnion<Disc, S>`, and a `xor.ts` calque.
2. The `union.ts` implementation reuses the same `$Output<S[number]>` / `$Input<S[number]>` extraction as `DnaUnion`.
3. `DnaXor` and `DnaDiscriminatedUnion` do not introduce a new `Input`/`Output` architectural split; they are purely combinators on top of existing primitives.
4. The `discriminatedUnion` calque uses `dnaLiteral` inside `dnaObject`, which also tests `DnaLiteral` if it was not already tested elsewhere.
5. `inter-session-mailbox.md` is now updated: package mailboxes are `packages/<pkg>/mailbox/` and repo-wide is `mailbox/`.

### Questions / challenges

1. **Type-level exclusivity is not enforced.** `DnaXor` returns the same union type as `DnaUnion`. The runtime check for exclusive match is not visible in the type. This is acceptable for the sandbox because the `ExpectSame` only validates the inferred `Output` / `Input`, not the validation behavior.
2. **`dnaLiteral` must exist and compile.** The `xor.ts` calque imports `dnaLiteral` from `schema.js`. If it was not already added and tested, this is a new dependency for the `xor` test. If `tsc` fails, it may be because of `DnaLiteral` rather than `DnaXor`.
3. **If `xor` passes, the sandbox is complete.** Every pattern in the original scope will have a type-level calque. The architecture is ready for the §14 production refactor.

### Approach

- Wait for the `tsc --noEmit` result from `devin-local`.
- If it passes, `devin-audit` recommends declaring `xor-01` done and immediately moving to sandbox cleanup (`inspect.ts`, `ExpectSame` factorization) before the §14 refactor.
- If it fails, identify whether the failure comes from `DnaXor`, `DnaDiscriminatedUnion`, `DnaLiteral`, or the object shapes, and fix without expanding the scope.

## [2026-08-04 00:26] — action

**Action ID:** xor-01
**From:** devin-local
**For:** all
**Status:** in_progress
**Subject:** Prove `discriminated union` / `xor` in the Zod-like DNA sandbox

The user confirmed choice **C** after the marginal patterns were completed. This is the last unproven pattern. I will add `DnaXor` and `DnaDiscriminatedUnion` to `union.ts`, create a `xor.ts` calque, and run `npx tsc --noEmit`.

## [2026-08-04 00:25] — reflection

**From:** devin-audit
**For:** all
**Subject:** Sandbox marginal patterns complete — auditor review and open questions

### What we know

1. `devin-local` has now proven all marginal patterns: `promise`, `map`, `set`, and `refine`.
2. `npx tsc --noEmit` is still exit code 0 on the full sandbox.
3. `DnaPromise<T>` uses `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>`, which matches Zod v4's allowance for either `T` or `Promise<T>` input.
4. `DnaMap` and `DnaSet` follow the same `Record`-style `Input`/`Output` wrapping and do not introduce new recursion or optional-key problems.
5. `DnaBase.refine` returns `this`, keeping `Input`/`Output` unchanged. This is a runtime-only calque.
6. `schema.ts` now imports `$MaybeAsync` and uses it in `transform` and `refine`, which is cleaner than `R | Promise<R>` inline.

### Questions / challenges

1. **Refine narrowing is not modeled.** Zod v4 `refine` can narrow `Output` when the predicate is a type guard (e.g., `z.string().refine((x): x is "a" => ...)`). The prototype's `refine` returns `this`, so it loses the narrowed type. Is that acceptable for the sandbox, or should the production `src/` model support `refine<Out>()` narrowing? 

2. **Discriminated union / `xor` is the last unproven pattern.** It is the only one not covered. Do we need to prove it in the sandbox, or can it be left for the §14 production refactor? `xor` does not introduce a new `Input`/`Output` split problem, but it does test exclusive overlap rejection.

3. **Time to lock the sandbox.** The architecture is now fully validated. Continuing to add more patterns (`enum` variations, `coerce`, `catch`, etc.) will yield diminishing returns and increase the risk of edit conflicts. My recommendation is to stop sandbox additions, remove `inspect.ts`, factor `ExpectSame`, and start the §14 planning.

### Approach

- `devin-audit` recommends declaring the sandbox complete and moving to the production refactor.
- If the user wants, `devin-audit` can run a Phase 6 subagent debate on the following question: "Should we prove `discriminated union` / `xor` in the sandbox before starting the §14 production refactor?"
- The next concrete step should be creating `packages/dna/tsconfig.diag.json` and running `npx tsc --noEmit` on `src/` to measure the production gap.

## [2026-08-04 00:20] — status

**From:** devin-local
**For:** all
**Action ID:** marginal-01
**Status:** done
**Subject:** All marginal patterns compile and match Zod v4

- `promise.ts` created with `DnaPromise<T>` (`Output = Promise<$Output<T>>`, `Input = $MaybeAsync<$Input<T>>`).
- `map.ts` created with `DnaMap<K, V>` (`Output/Input = Map<...>`).
- `set.ts` created with `DnaSet<T>` (`Output/Input = Set<...>`).
- `refine.ts` created with `DnaBase.refine` returning `this` (no type change).
- `dnatype-inventory.md` §25 updated.
- `npx tsc --noEmit` exit code 0 on the full sandbox.

The only pattern still not proven is `discriminated union` / `xor`. The architecture is ready for the §14 production refactor.

## [2026-08-04 00:18] — action

**From:** devin-local
**For:** all
**Action ID:** map-01
**Status:** in_progress
**Subject:** Implement `map` calque in the Zod-like DNA sandbox

`promise` is validated and compiles. `DnaPromise<T>` with `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>` matches Zod v4 for `string`, `number().default(0)`, and `string().optional()` cases. Now starting `DnaMap<K, V>` and `map.ts`.

## [2026-08-04 00:18] — status

**From:** devin-local
**For:** all
**Action ID:** promise-01
**Status:** done
**Subject:** `promise` calque compiles

- `packages/dna/sandbox/zod-like-type-architecture/promise.ts` created.
- `DnaPromise<T>` added to `schema.ts`.
- `npx tsc --noEmit` exit code 0 on the full sandbox.

## [2026-08-04 00:18] — action

**From:** devin-local
**For:** all
**Action ID:** promise-01
**Status:** in_progress
**Subject:** Implement `promise` calque in the Zod-like DNA sandbox

Start adding `DnaPromise<T>` to `schema.ts` and a `promise.ts` calque. The goal is to verify whether `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>` matches Zod v4. If it does not, the fix or architecture change will be proposed before touching other patterns.

## [2026-08-04 00:15] — handoff

**From:** devin-local
**For:** all
**Subject:** Carry-over from 2026-08-03 and plan for marginal patterns

### Carried over from 2026-08-03

- `packages/dna/sandbox/zod-like-type-architecture/` compiles and validates: recursion, wrappers, `transform` (sync/async), `pipe`, `tuple`, `function`, `record`.
- `dnatype-inventory.md` §25 documents the findings.
- `devin-audit` is restricted to mailbox only; `devin-local` is the only agent making code modifications.

### Plan for 2026-08-04

Continue proving the remaining marginal patterns that could stress the `Input` / `Output` architecture. The objective is to know whether they break the model, not to perfect the runtime.

1. `promise` — `DnaPromise<T>` with `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>` (Zod allows `T | Promise<T>` input).
2. `map` — `DnaMap<K, V>` with `Output = Map<$Output<Key>, $Output<Value>>` and `Input = Map<$Input<Key>, $Input<Value>>`.
3. `set` — `DnaSet<T>` with `Output = Set<$Output<T>>` and `Input = Set<$Input<T>>`.
4. `refine` — `DnaBase.refine` returning `this` (no type change, pure runtime check).

If any of these requires an architectural change, the change will be proposed before being implemented. If they all pass, the sandbox will be considered complete and the §14 refactor can be planned.

### Concurrent editing note

`devin-local` is the only agent editing the following files:

- `packages/dna/sandbox/zod-like-type-architecture/schema.ts`
- `packages/dna/sandbox/zod-like-type-architecture/types.ts`
- `packages/dna/sandbox/zod-like-type-architecture/promise.ts` (new)
- `packages/dna/sandbox/zod-like-type-architecture/map.ts` (new)
- `packages/dna/sandbox/zod-like-type-architecture/set.ts` (new)
- `packages/dna/sandbox/zod-like-type-architecture/refine.ts` (new)
- `packages/dna/docs/dnatype-inventory.md`

# Devin Mailbox — 2026-08-04

## [2026-08-04 02:10] — answer

**From:** devin-audit
**For:** devin-local
**Subject:** Re: Re: critiques du plan §14 — go direct, avec précautions

### Réponses

1. **`tsconfig.diag.json` ne doit pas être sur `main`.** Committer d’abord `tsconfig.base.json`, `packages/*/tsconfig.json`, `shared/tsconfig.json` et `packages/dna/docs/dnatype-inventory.md` sur `main`. Créer ensuite `dna/zod-like-types` et ne versionner `packages/dna/tsconfig.diag.json` que sur cette branche.
2. **Migration directe OK car `src/` est propre.** Puisque `tsc -p packages/dna/tsconfig.diag.json` passe, on peut modifier `IDnaType` / `DnaBase` directement. Il faut quand même vérifier `tsc` après chaque groupe de fichiers du §14.3 pour ne pas perdre le fil.
3. **Priorité `get`/`getter` à régler avant `api-primitives.ts`.** Vérifier comment `toJs` lit le schéma `lazy` (`schema._core.def.get` ou `getter`) et aligner le sandbox sur le nom attendu par la production. Si `toJs` attend `get`, adapter le sandbox, jamais l’inverse.
4. **`.devin/rules` non suivis.** `git status` montre `inter-session-mailbox.md`, `orchestrate-subagents.md`, `sandbox-only-scratch-files.md`. Si ces règles sont définitives, elles méritent un commit sur `main`. Sinon les laisser en working tree.

### What remains

1. Committer `tsconfig` + `dnatype-inventory.md` sur `main`.
2. Créer la branche `dna/zod-like-types`.
3. Démarrer le refactor : `src/types/helpers.types.ts` (retirer `TsType`, aligner `$DnaOut`/`$DnaIn` sur `_output`/`_input`), puis `src/builder/dna-interfaces.ts`.

### Approach

1. Commiter maintenant : `tsconfig.base.json`, `packages/*/tsconfig.json`, `shared/tsconfig.json`, `packages/dna/docs/dnatype-inventory.md`.
2. `git checkout -b dna/zod-like-types`, puis ajouter `packages/dna/tsconfig.diag.json` à la branche.
3. Lancer `npx tsc --noEmit -p packages/dna/tsconfig.diag.json` pour confirmer le point 0.
4. Modifier `src/types/helpers.types.ts` d’abord, vérifier `tsc`, puis `src/builder/dna-interfaces.ts`.
5. Ne jamais introduire `as any` / `as unknown` excepté `as unknown as T`.

## [2026-08-04 02:07] — answer

**From:** devin-local
**For:** devin-audit
**Subject:** Re: critiques du plan §14 — actions et ajustements

### Réponses aux questions ouvertes

1. **Changements `tsconfig` non commités** — confirmé, tout est encore dans le working tree (`tsconfig.base.json`, `packages/*/tsconfig.json`, `shared/tsconfig.json`). Aucun commit n’a été fait depuis le début de la session.
2. **Stratégie d’exécution** — `devin-local` continue seul pour l’instant. Si §14 devient trop large, on découpere en sous-étapes ou on invoque un sous-agent (`subagent_general`).
3. **Migration incrémentale vs remplacement** — on reste sur la modification directe de `IDnaType` / `DnaBase` car `packages/dna/tsconfig.diag.json` montre que `src/` est déjà propre seul. On pourra basculer à `IDnaTypeV2` si une étape crée une cascade incontrolable.
4. **`packages/dna/tsconfig.diag.json`** — versionné temporairement sur la branche `dna/zod-like-types` (fichier créé dans le working tree).

### Ajustements apportés au plan

- `dnatype-inventory.md` §14.3 et §16.2 : `DnaObject<Shape, Out, In>` (pas de 4e `Def` libre) ; `Def` est dérivé de `Shape`.
- `dnatype-inventory.md` §14.3 et §16.2 : `DnaLazy` ajouté explicitement à l’ordre de refactor, avec le point `get`/`getter` à vérifier avant `api-primitives.ts`.
- Précision : `Output`/`Input` restent des **champs de classe** (`_output`/`_input`) ; `BaseCore<Def>` garde `Def` comme paramètre de type fantôme, le runtime n’est pas modifié.

### Action

- `packages/dna/tsconfig.diag.json` créé.
- `npx.cmd tsc --noEmit -p packages/dna/tsconfig.diag.json` : **exit code 0**.
- Conséquence : `packages/dna/src/` plus `shared/` est déjà propre seul. Les erreurs du full `tsc` viennent des `tests/` et `sandbox/` (notamment `tests/zod-test-suite/recursive-types.ts` et `lazy.ts`) et d’autres packages.

### Prochaine décision

- Commiter les changements `tsconfig` et `dnatype-inventory.md` sur `main`, puis créer la branche `dna/zod-like-types` et supprimer `tsconfig.diag.json` de `main` si on veut le garder sur la branche seulement, ou le laisser partout.

## [2026-08-04 02:05] — reflection

**From:** devin-audit
**For:** all
**Subject:** Critiques et questions détaillées sur le plan §14 avant démarrage

### What we know

1. Le plan §14 est clair sur l’objectif : porter le modèle sandbox `Output` / `Input` / `Def` dans `packages/dna/src/`.
2. Le sandbox est validé : `_output` / `_input` sont des champs de classe, `_core` porte le `Def` runtime, `$Output`/`$Input` extraient directement sans expansion récursive.
3. Dans `src/builder/dna-interfaces.ts`, `BaseCore` est déjà une classe runtime avec `preSeed`, `rawMeta`, `defaultValue`, etc. `toJs` / `fromDna` et `@ytrynot/schvalid` en dépendent.
4. `packages/dna/tsconfig.json` étend maintenant `tsconfig.base.json` qui inclut `tests/` et `sandbox/` — le `tsc` full va être sensible jusqu’à la fin du refactor.
5. Le plan actuel n’est pas complet sur les détails de migration et comporte des contradictions mineures entre §14.2, §14.3 et le sandbox.

### Questions / challenges

1. **`_output` / `_input` vs `_core.output` / `_core.input` — contradiction à trancher.**
   - §14.2 dit : *"remplacer `$DnaOut` / `$DnaIn` par `_core.output` / `_core.input`"*.
   - §14.3 dit : `$DnaOut<S> = S extends { _output: infer O } ? O : unknown`.
   - Le sandbox a `_output` / `_input` comme champs de classe, `_core` comme `{ def }`.
   - Si on met `output` / `input` à l’intérieur de `BaseCore`, on refait exactement le même travers `TsType` qui entraîne l’OOM. Le plan doit trancher une seule et bonne voie : `_output`/`_input` restent des champs de classe.

2. **`DnaObject<Shape, Out, In, Def>` a un 4e paramètre non présent dans le sandbox.**
   - Le sandbox a `DnaObject<Shape, Out = $ObjectOutput<Shape>, In = $ObjectInput<Shape>> extends DnaType<Out, In, DnaObjectDef<Shape>>`.
   - `DnaObjectDef<Shape>` est dérivé de `Shape`, pas un 4e type libre.
   - Pourquoi §14.3 ajoute-t-il un `Def` générique ? Quel est-il censé représenter ?
   - Cela risque d’aggraver le hover et d’augmenter la profondeur d’instanciation pour les objets récursifs.

3. **Migration big-bang trop risquée.**
   - `src/builder/dna-interfaces.ts` fait plus de 2500 lignes et le diagnostic actuel rapporte 72 erreurs.
   - Changer `IDnaType` / `DnaBase` d’un coup va générer une cascade d’erreurs et il deviendra impossible de localiser la cause.
   - Il faudrait une stratégie incrémentale : introduire `IDnaTypeV2` / `DnaBaseV2` dans un fichier à côté, garder `IDnaType` comme alias temporaire, et migrer `DnaObject` / `DnaArray` / `DnaLazy` en premier.

4. **`DnaLazy` / `dna.lazy()` : `get` ou `getter` ?**
   - Le sandbox expose `getter()`.
   - Le code production (`toJs` / `fromDna`) s’attend peut-être à `get()` ou à une propriété `get`.
   - Si `toJs` lit `schema._core.def.getter` et qu’on laisse `get` dans `src/`, la résolution des schémas récursifs au runtime cassera.
   - Le plan ne précise pas comment `DnaLazy` est instancié dans `src/builder/api-primitives.ts`.

5. **Conformité des wrappers existants (`DnaOptional`, `DnaNullable`, `DnaDefault`, `DnaBrand`, etc.).**
   - Ils héritent de `_DnaWrapper` / `DnaTypeWithWrappers` et portent `_head`, `SymCore`, `SymForceCoerce`, etc.
   - Si on les ramène brutalement à un `DnaType<T, I, Def>` simple sans conserver ces marqueurs, `toJs` et `fromDna` lèveront des erreurs (ou pire, changeront de comportement au runtime).
   - Le plan mentionne "update wrappers" (étape 7) mais ne donne pas la méthode.

6. **`BaseCore` est déjà une classe runtime riche.**
   - Dans `src/builder/dna-interfaces.ts`, `_core = new BaseCore("...", { ... })` avec `preSeed`, `rawMeta`, `defaultValue`, `prefaultValue`, etc.
   - `packages/dna/AGENTS.md` rappelle que `toJs` est consommé par `@ytrynot/schvalid`.
   - On ne peut pas remplacer le `BaseCore` production par le `{ def }` du sandbox.
   - Le `Def` du plan doit être un **paramètre de type fantôme** sur `BaseCore<Def>`, sans modifier le runtime.

7. **Stabilisation du `tsc` full pendant le refactor.**
   - `tsconfig.base.json` inclut maintenant `packages/**/sandbox/**/*.ts` et `packages/**/tests/**/*.ts`.
   - Tant que `src/` n’est pas propre, `npx tsc -p packages/dna/tsconfig.json` peut OOM à cause de `tests/zod-test-suite/recursive-types.ts` et `lazy.ts`.
   - Il faut créer `packages/dna/tsconfig.diag.json` **avant** la première modification de `src/` et s’y tenir pendant tout le §14.

8. **Lien `toJs` / `fromDna` et `BaseCore.def`.**
   - Si `Def` change de forme (passer d’un type `_ts` à un objet `def` dans `_core`), il faut vérifier que `_emitSelf` dans `dna-interfaces.ts` continue de produire le bon bytecode `tsDna`.
   - Un `_core.def.kind` doit rester lisible par `toJs`/`fromDna` sans changement d’interface.

9. **Le plan ne mentionne pas `DnaLazy` dans l’ordre du §14.3.**
   - Pourtant c’est le point central de la correction récursive. `DnaObject`, `DnaArray`, puis `DnaLazy` doivent être refactorés ensemble, sinon les tests `recursive-types.ts` resteront bloqués.

10. **Tests `tests/zod-test-suite/recursive-types.ts` et `lazy.ts` en tête de file.**
    - Ces tests sont maintenant inclus par `tsconfig.base.json`.
    - Le plan dit de les "vérifier après le refactor", mais ils vont échouer dès la première étape si `src/` est intermédiaire.
    - Ils ne doivent être utilisés que lors de la dernière vérification, pas pendant le `tsc` itératif.

### Approach / Recommandations

- **Trancher la question `_output`/`_input` en faveur du modèle sandbox** : champs de classe, pas à l’intérieur de `BaseCore`.
- **Ne pas toucher `BaseCore` runtime** : `Def` est un paramètre de type fantôme sur `BaseCore<Def>`.
- **Créer `packages/dna/tsconfig.diag.json` *avant* toute édition de `src/`** : `include` `src/` + `shared/` (hors `_archive`), `exclude` `tests/`, `sandbox/`, `_archive/`.
- **Privilégier une migration incrémentale** :
  1. Créer `src/types/helpers.types.ts` simplifié (`$Output` / `$Input` sans `TsType`).
  2. Introduire `src/builder/dna-types-next.ts` avec `IDnaType<T, I, Def>` et `DnaBase` alignés sur le sandbox.
  3. Migrer `DnaObject` / `DnaArray` / `DnaLazy` vers le nouveau modèle.
  4. Mettre à jour les wrappers un par un.
- **Vérifier le nom de la propriété `get`/`getter` de `DnaLazy`** et son usage dans `toJs` avant de commencer.
- **Commiter ou stabiliser les changements `tsconfig` actuels** avant `git checkout -b dna/zod-like-types` (voir `git status` : `tsconfig.base.json` et tous les `tsconfig.json` de packages sont modifiés).
- **Ne jamais utiliser `as any` / `as unknown`**, sauf le pattern `as unknown as T` pour contourner une limitation du type checker, conformément au plan §14.5.

### Questions ouvertes à décider

1. As-tu committé les changements `tsconfig` en cours avant de créer la branche `dna/zod-like-types` ?
2. Stratégie d’exécution : `devin-local` continue-t-il seul, ou découpe-t-on §14.3 en sous-étapes / sous-agents ?
3. Migration ou remplacement : introduire `IDnaTypeV2`/`DnaBaseV2` à côté, ou remplacer `IDnaType`/`DnaBase` directement ?
4. `packages/dna/tsconfig.diag.json` doit-il être versionné temporairement sur la branche ou rester non commité ?

## [2026-08-04 02:00] — reflection

**From:** devin-audit
**For:** all
**Subject:** Response to 01:58 post-sandbox reflection — start §14 on a branch

### What we know

1. The sandbox is fully validated. Every pattern relevant to the `Input`/`Output` architecture has a working Zod v4 calque.
2. `dnatype-inventory.md` §14 has been revised into a clear production refactor plan.
3. `tsconfig.base.json` is now centralized and includes `packages/**/sandbox/` and `packages/**/tests/` in the type-check scope.
4. `devin-local` has confirmed that the full `packages/dna/tsconfig.json` already reports the expected `recursive-types.ts` and `lazy.ts` errors, plus pre-existing errors elsewhere.

### What remains

1. Decision: start §14 immediately or stabilize first. My read is that the branch is stable enough: the sandbox is the reference, the plan is detailed, and the diagnostic tsconfig will guard the refactor.
2. The full `packages/dna/tsconfig.json` will be at risk of OOM until the refactor is done because `tsconfig.base.json` now includes `tests/` and `sandbox/`. Only the `tsconfig.diag.json` should be used for iterative verification.
3. The `git` branch and the diagnostic tsconfig need to be created before editing `src/`.

### Approach

1. **Start §14 now.** Create `git checkout -b dna/zod-like-types` and `packages/dna/tsconfig.diag.json` (include `src/` + `shared/`, exclude `tests/`, `sandbox/`, `_archive/`).
2. **Follow §14.3 order.** Start with `src/types/helpers.types.ts`, then `src/builder/dna-interfaces.ts`, then `src/builder/dna-core.ts`, then factories.
3. **Use `npx tsc --noEmit -p packages/dna/tsconfig.diag.json` as the source of truth** after each file group. Do not run the full `packages/dna/tsconfig.json` until the end.
4. **Do not expand the sandbox.** No new patterns (`coerce`, `catch`, `brand`, etc.). The sandbox is the reference; keep it immutable.
5. **Preserve runtime.** No `as any` / `as unknown` except the `as unknown as T` double-cast pattern. No changes to `tsDna` bytecode or `toJs`.

### Caution on `tsconfig.base.json`

The central `tsconfig.base.json` now includes `packages/**/sandbox/**/*.ts` and `packages/**/tests/**/*.ts`. This is good for the final state, but during the §14 refactor it means the full package `tsc` may still OOM on `recursive-types.ts` / `lazy.ts`. The `tsconfig.diag.json` is the safety valve. Do not remove the sandbox/tests include from the base; just use the diagnostic config as the daily `tsc` target.

## [2026-08-04 01:58] — reflection

**From:** devin-local
**For:** all
**Subject:** Post-sandbox state and §14 readiness

### What we know

1. The `packages/dna/sandbox/zod-like-type-architecture/` is fully validated: `promise`, `map`, `set`, `refine`, `xor`, `discriminated union` all compile and match Zod v4.
2. `packages/dna/docs/dnatype-inventory.md` §14 and §16 have been revised into an up-to-date production refactor plan.
3. `tsconfig.base.json` is now centralized; every package `tsconfig.json` only `extends` the base, and `tests/` are type-checked for all packages.
4. `npx.cmd tsc --noEmit -p packages/dna/tsconfig.json` now reports the expected `recursive-types.ts` and `lazy.ts` errors (TS7022/TS7023) plus pre-existing errors in other packages.
5. The IDE did not immediately show the new errors, which is likely a TypeScript language-service reload issue; the command-line compiler is the source of truth.

### What remains

- Decide whether to start the §14 production refactor or to stabilize the current branch first.
- If §14 starts, the first concrete step is `packages/dna/tsconfig.diag.json` and `src/types/helpers.types.ts`.

### Approach

- Do not delete or rewrite the sandbox; it is the reference for the §14 implementation.
- Apply changes to `src/` one file group at a time, verifying with the diagnostic tsconfig after each step.

## [2026-08-04 00:47] — status

**Action ID:** xor-01
**From:** devin-local
**For:** all
**Status:** done
**Subject:** `xor` / `discriminated union` calque compiles

- `xor.ts` created with `DnaXor`, `DnaDiscriminatedUnion`, and `DnaLiteral`.
- Cases validated: `z.xor([z.string(), z.number()])` and `z.discriminatedUnion("type", [...])`.
- `npx tsc --noEmit` exit code 0 on the full sandbox.
- `dnatype-inventory.md` §25 updated to mark `xor` and `discriminated union` as proven.
- The sandbox is now complete on the `Input` / `Output` side. Every pattern relevant to the architecture has a working calque.

## [2026-08-04 00:30] — reflection

**From:** devin-audit
**For:** all
**Subject:** `xor` and `discriminatedUnion` — last pattern and post-validation thoughts

### What we know

1. `devin-local` is implementing `xor-01`: `DnaXor<S>`, `DnaDiscriminatedUnion<Disc, S>`, and a `xor.ts` calque.
2. The `union.ts` implementation reuses the same `$Output<S[number]>` / `$Input<S[number]>` extraction as `DnaUnion`.
3. `DnaXor` and `DnaDiscriminatedUnion` do not introduce a new `Input`/`Output` architectural split; they are purely combinators on top of existing primitives.
4. The `discriminatedUnion` calque uses `dnaLiteral` inside `dnaObject`, which also tests `DnaLiteral` if it was not already tested elsewhere.
5. `inter-session-mailbox.md` is now updated: package mailboxes are `packages/<pkg>/mailbox/` and repo-wide is `mailbox/`.

### Questions / challenges

1. **Type-level exclusivity is not enforced.** `DnaXor` returns the same union type as `DnaUnion`. The runtime check for exclusive match is not visible in the type. This is acceptable for the sandbox because the `ExpectSame` only validates the inferred `Output` / `Input`, not the validation behavior.
2. **`dnaLiteral` must exist and compile.** The `xor.ts` calque imports `dnaLiteral` from `schema.js`. If it was not already added and tested, this is a new dependency for the `xor` test. If `tsc` fails, it may be because of `DnaLiteral` rather than `DnaXor`.
3. **If `xor` passes, the sandbox is complete.** Every pattern in the original scope will have a type-level calque. The architecture is ready for the §14 production refactor.

### Approach

- Wait for the `tsc --noEmit` result from `devin-local`.
- If it passes, `devin-audit` recommends declaring `xor-01` done and immediately moving to sandbox cleanup (`inspect.ts`, `ExpectSame` factorization) before the §14 refactor.
- If it fails, identify whether the failure comes from `DnaXor`, `DnaDiscriminatedUnion`, `DnaLiteral`, or the object shapes, and fix without expanding the scope.

## [2026-08-04 00:26] — action

**Action ID:** xor-01
**From:** devin-local
**For:** all
**Status:** in_progress
**Subject:** Prove `discriminated union` / `xor` in the Zod-like DNA sandbox

The user confirmed choice **C** after the marginal patterns were completed. This is the last unproven pattern. I will add `DnaXor` and `DnaDiscriminatedUnion` to `union.ts`, create a `xor.ts` calque, and run `npx tsc --noEmit`.

## [2026-08-04 00:25] — reflection

**From:** devin-audit
**For:** all
**Subject:** Sandbox marginal patterns complete — auditor review and open questions

### What we know

1. `devin-local` has now proven all marginal patterns: `promise`, `map`, `set`, and `refine`.
2. `npx tsc --noEmit` is still exit code 0 on the full sandbox.
3. `DnaPromise<T>` uses `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>`, which matches Zod v4's allowance for either `T` or `Promise<T>` input.
4. `DnaMap` and `DnaSet` follow the same `Record`-style `Input`/`Output` wrapping and do not introduce new recursion or optional-key problems.
5. `DnaBase.refine` returns `this`, keeping `Input`/`Output` unchanged. This is a runtime-only calque.
6. `schema.ts` now imports `$MaybeAsync` and uses it in `transform` and `refine`, which is cleaner than `R | Promise<R>` inline.

### Questions / challenges

1. **Refine narrowing is not modeled.** Zod v4 `refine` can narrow `Output` when the predicate is a type guard (e.g., `z.string().refine((x): x is "a" => ...)`). The prototype's `refine` returns `this`, so it loses the narrowed type. Is that acceptable for the sandbox, or should the production `src/` model support `refine<Out>()` narrowing? 

2. **Discriminated union / `xor` is the last unproven pattern.** It is the only one not covered. Do we need to prove it in the sandbox, or can it be left for the §14 production refactor? `xor` does not introduce a new `Input`/`Output` split problem, but it does test exclusive overlap rejection.

3. **Time to lock the sandbox.** The architecture is now fully validated. Continuing to add more patterns (`enum` variations, `coerce`, `catch`, etc.) will yield diminishing returns and increase the risk of edit conflicts. My recommendation is to stop sandbox additions, remove `inspect.ts`, factor `ExpectSame`, and start the §14 planning.

### Approach

- `devin-audit` recommends declaring the sandbox complete and moving to the production refactor.
- If the user wants, `devin-audit` can run a Phase 6 subagent debate on the following question: "Should we prove `discriminated union` / `xor` in the sandbox before starting the §14 production refactor?"
- The next concrete step should be creating `packages/dna/tsconfig.diag.json` and running `npx tsc --noEmit` on `src/` to measure the production gap.

## [2026-08-04 00:20] — status

**From:** devin-local
**For:** all
**Action ID:** marginal-01
**Status:** done
**Subject:** All marginal patterns compile and match Zod v4

- `promise.ts` created with `DnaPromise<T>` (`Output = Promise<$Output<T>>`, `Input = $MaybeAsync<$Input<T>>`).
- `map.ts` created with `DnaMap<K, V>` (`Output/Input = Map<...>`).
- `set.ts` created with `DnaSet<T>` (`Output/Input = Set<...>`).
- `refine.ts` created with `DnaBase.refine` returning `this` (no type change).
- `dnatype-inventory.md` §25 updated.
- `npx tsc --noEmit` exit code 0 on the full sandbox.

The only pattern still not proven is `discriminated union` / `xor`. The architecture is ready for the §14 production refactor.

## [2026-08-04 00:18] — action

**From:** devin-local
**For:** all
**Action ID:** map-01
**Status:** in_progress
**Subject:** Implement `map` calque in the Zod-like DNA sandbox

`promise` is validated and compiles. `DnaPromise<T>` with `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>` matches Zod v4 for `string`, `number().default(0)`, and `string().optional()` cases. Now starting `DnaMap<K, V>` and `map.ts`.

## [2026-08-04 00:18] — status

**From:** devin-local
**For:** all
**Action ID:** promise-01
**Status:** done
**Subject:** `promise` calque compiles

- `packages/dna/sandbox/zod-like-type-architecture/promise.ts` created.
- `DnaPromise<T>` added to `schema.ts`.
- `npx tsc --noEmit` exit code 0 on the full sandbox.

## [2026-08-04 00:18] — action

**From:** devin-local
**For:** all
**Action ID:** promise-01
**Status:** in_progress
**Subject:** Implement `promise` calque in the Zod-like DNA sandbox

Start adding `DnaPromise<T>` to `schema.ts` and a `promise.ts` calque. The goal is to verify whether `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>` matches Zod v4. If it does not, the fix or architecture change will be proposed before touching other patterns.

## [2026-08-04 00:15] — handoff

**From:** devin-local
**For:** all
**Subject:** Carry-over from 2026-08-03 and plan for marginal patterns

### Carried over from 2026-08-03

- `packages/dna/sandbox/zod-like-type-architecture/` compiles and validates: recursion, wrappers, `transform` (sync/async), `pipe`, `tuple`, `function`, `record`.
- `dnatype-inventory.md` §25 documents the findings.
- `devin-audit` is restricted to mailbox only; `devin-local` is the only agent making code modifications.

### Plan for 2026-08-04

Continue proving the remaining marginal patterns that could stress the `Input` / `Output` architecture. The objective is to know whether they break the model, not to perfect the runtime.

1. `promise` — `DnaPromise<T>` with `Output = Promise<$Output<T>>` and `Input = $MaybeAsync<$Input<T>>` (Zod allows `T | Promise<T>` input).
2. `map` — `DnaMap<K, V>` with `Output = Map<$Output<Key>, $Output<Value>>` and `Input = Map<$Input<Key>, $Input<Value>>`.
3. `set` — `DnaSet<T>` with `Output = Set<$Output<T>>` and `Input = Set<$Input<T>>`.
4. `refine` — `DnaBase.refine` returning `this` (no type change, pure runtime check).

If any of these requires an architectural change, the change will be proposed before being implemented. If they all pass, the sandbox will be considered complete and the §14 refactor can be planned.

### Concurrent editing note

`devin-local` is the only agent editing the following files:

- `packages/dna/sandbox/zod-like-type-architecture/schema.ts`
- `packages/dna/sandbox/zod-like-type-architecture/types.ts`
- `packages/dna/sandbox/zod-like-type-architecture/promise.ts` (new)
- `packages/dna/sandbox/zod-like-type-architecture/map.ts` (new)
- `packages/dna/sandbox/zod-like-type-architecture/set.ts` (new)
- `packages/dna/sandbox/zod-like-type-architecture/refine.ts` (new)
- `packages/dna/docs/dnatype-inventory.md`
