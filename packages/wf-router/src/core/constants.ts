
/** Hidden Symbol to mark valid gate results and prevent tampering. */
export const sGateResult = Symbol("GateResult");

/** Cosntants for 'terminal step ID' */
export const TERMINAL_STEP_ID = "__terminal__" as const;